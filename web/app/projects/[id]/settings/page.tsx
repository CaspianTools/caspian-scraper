import { ComingSoon } from "@/components/ComingSoon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Edit project name, description, schedule. Pause / resume the project. Delete (with confirmation)."
    />
  );
}
