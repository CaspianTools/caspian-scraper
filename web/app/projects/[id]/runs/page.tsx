import { ComingSoon } from "@/components/ComingSoon";

export default function RunsPage() {
  return (
    <ComingSoon
      title="Runs"
      description="Run history with per-source breakdown, errors, and ad-hoc trigger. Each scheduled or manual run produces a record here."
    />
  );
}
